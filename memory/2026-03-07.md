# 2026-03-07 — Saturday

## Dashboard work — klynstras-2026 (HA)

Big session building out new views for the klynstras-2026 dashboard. Style guide: `config/dashboards/klynstras-2026/klynstra-2026-dashboard-style-guide.md`. All views use `custom:tailwindcss-template-card` with dark glassmorphic cards, sections layout, `max_columns: 3`.

### People view (`views/people.yaml`)
- Three sections: Michael, Candy, Archer
- Michael: presence (iCloud3), iPhone (battery/focus/steps), MacBook Pro (active/frontmost app/camera/mic), Apple Watch
- Candy: presence, iPhone, Watch, Office card (lights/door/device)
- Archer: presence, iPhone 13 Pro (internet blocked/data killed status), Profile & Screentime, Medicine (last taken timestamps)
- Color coding: blue=Michael, pink=Candy, cyan=Archer, green=home, amber=away

### Lighting view (`views/lighting.yaml`)
- First draft used individual bulbs — Michael flagged this
- **Key lesson: use AREA group lights as primary, not individual bulbs**
- Rebuilt using `area_entities()` in Jinja + HA area group lights as top-level controls
- Sections: Whole Home (master controls + zone toggles), Living Area, Kitchen & Dining, Bedrooms, Offices, Downstairs, Exterior
- Master Controls uses `script.lights_out_activate` / `script.lights_out_deactivate`
- Michael's office includes Groovy scene, Cololight, desktop LED strip
- Porch sub-fixtures loop over `area_entities('porch')` dynamically

### Areas confirmed in HA
`archers_bathroom`, `archers_bedroom`, `back_yard`, `bathroom`, `bedroom`, `billiard_area`, `candys_office`, `dining_room`, `downstairs`, `downstairs_peloton_area`, `downstairs_tv`, `downstairs_tv_area`, `driveway`, `front`, `garage`, `hallway`, `hallway_small`, `kitchen`, `living_room`, `michaels_office`, `mudroom`, `office`, `outside`, `porch`, `powder_room`, `stairway`

## Voice on
- TTS enabled this session, ElevenLabs backend via http://127.0.0.1:8200
- Weather delivered by voice: 55°F misty, SSW 23mph, high 61, rain AM clearing PM

## Dashboard bug fix — localhost:8008 (mknotes)
- Dashboard not loading: `dashboard.js` was never included in `dashboard.html`
- Added `<script src="/static/dashboard.js" defer></script>`
- Also fixed: inline `refreshHeadlines()` was calling `loadDashboard()` (doesn't exist) — corrected to `fetchAndRender()`
- Server: uvicorn, PID 11160, `C:\Mount\Projects\mknotes`
- API endpoint: `/dashboard/api/data` (GET) — was working fine, just JS never loaded

## Michael's dashboard (localhost:8008) — mknotes
- Custom personal dashboard: tasks, focuses, cameras, emails, Archer screentime, calendar, activity, AI headlines
- Built on FastAPI/uvicorn
- Separate from HA — this is his productivity/home-base dashboard
